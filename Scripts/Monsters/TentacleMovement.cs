using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TentacleMovement : MonoBehaviour
{
    BoxCollider movementZone;
    // Start is called before the first frame update
    void Start()
    {
        movementZone.bounds;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
